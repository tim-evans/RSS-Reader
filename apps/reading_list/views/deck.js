// ==========================================================================
// Project:   ReadingList.DeckView
// Copyright: @2012 Tim Evans
// ==========================================================================
/*globals ReadingList */

/**
  A deck holds a bunch of {@link ReadingList.CardView}s
  and manages them in a way so gestures apply to the whole
  deck.
  @extend SC.View
 */
ReadingList.DeckView = SC.View.extend(
  /** @scope ReadingList.DeckView.prototype */{

});
